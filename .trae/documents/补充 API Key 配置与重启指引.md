# 完善使用文档 (Finalize Documentation)

## 目标

在 README.md 中补充关于“API Key 配置与服务重启”的操作指引，解决用户在首次配置后的困惑。

## 变更计划

### 编辑 `README.md`

1. 在“快速开始”章节后，新增 **“🔑 配置与重启”** 子章节。
2. **补充内容**：

   * **Key 的获取与填写**: 简要说明在 `server/.env` 中填入 Key。

   * **生效机制**: 说明后端服务需重启才能加载新环境变量。

   * **操作命令**:

     * **一键脚本用户**: 推荐 `Ctrl+C` 停止后重新运行 `./setup_and_run.sh`。

     * **Codespaces 用户**: 说明同样适用一键脚本重启，或使用终端命令 `pkill node` 杀进程。

